# Normaliza saltos de línea: el repo siempre guarda LF.
* text=auto eol=lf

# Scripts de shell SIEMPRE en LF (si quedan en CRLF, fallan dentro de Docker/Linux).
*.sh text eol=lf
scripts/entrypoint.sh text eol=lf

# Scripts específicos de Windows en CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
